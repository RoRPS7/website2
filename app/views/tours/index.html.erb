<p id="notice"><%= notice %></p>

<h1>Przykładowe trasy</h1>
<p>Zamiast tworzyć trasę od zera możesz wykorzystać przykładowe trasy z atrakcjami poniżej.</p>

<table>
  <thead>
    <tr>
      <th>Lista przykładowych tras</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tours.each do |tour| %>
      <tr>
        <td><% tour.attractions.each do |attraction| %>
          <%= attraction.name %>
          <% end %>
            </td>
        <td><%= link_to 'Show', tour %></td>
        <td><%= link_to 'Edit', edit_tour_path(tour) %></td>
        <td><%= link_to 'Destroy', tour, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Tour', new_tour_path %>
<%= link_to 'Main Page', root_path %>
