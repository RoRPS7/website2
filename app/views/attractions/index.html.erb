<p id="notice"><%= notice %></p>

<h1> Wybierz swoje atrakcje! </h1>
<p>
  Poniżej zamieszczono wszystkie atrakcje w Białymstoku i okolicy! Możesz się z nimi zapoznać
  klikając w przycisk "Pokaż", lub od razu dodać do koszyka!
</p>
<table>
  <thead>
    <tr>
      <th>Nazwa obiektu</th>
      <th>Oplata</th>
      <th>Godziny otwarcia</th>
      <th>Kategoria</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @attractions.each do |attraction| %>
      <tr>
        <td><%= attraction.name %></td>
        <td><% if attraction.price > 0%>
            <strong>Opłata:</strong>
            <%= attraction.price %>
          <%else %>
            <strong>Obiekt bezpłatny</strong>
          <%end %></td>
        <td><%= attraction.time %></td>
        <td><% attraction.categories.each do |category| %>
            <%= category.name %>
            <% end %>
            </td>
        <td class="btn btn-info"><%= link_to 'Pokaż', attraction %></td>
        <td><button class="btn btn-success">Dodaj do koszyka!</button></td>
<!--        <td><%#= link_to 'Edit', edit_attraction_path(attraction) %></td>-->
<!--        <td><%#= link_to 'Destroy', attraction, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New Attraction', new_attraction_path %>
<%= link_to 'Powrót do strony głównej', root_path %>